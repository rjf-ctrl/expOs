type
    Student
    {
        str name;
        int marks;
    }
endtype

int main()
{
decl
    Student s;
    int temp;
enddecl
begin
    // Allocate memory for Student variable
    s = alloc();

    // Assign values
    s.name = "John";
    s.marks = 89;

    // Print values
    temp = exposcall("Write", -2, s.name);
    temp = exposcall("Write", -2, s.marks);

    return 0;
end
}
